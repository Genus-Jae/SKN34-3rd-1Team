# 지역 OR 대안 보완 후 실제 재검증

2026-09-07 UTC 10:56:25~10:56:45, `gpt-5.6-luna`/추론 none/랭킹 45·50초.
사용자가 남은 2회를 원래의 검색(랭킹+임베딩) 대신 서울·부산 합성 재검증으로 전환 승인했다.
이 실행은 정확히 랭킹 2회, 임베딩 0회, 자동 재시도 0회다. v1과 합쳐 이번 실제 API는 총 5회다.

현재 소재지 경로를 충족하면 다른 이전 경로를 추가로 요구하지 않고, 소재지 불충족과 이전 의사
미확인의 조합은 UNKNOWN으로 다루도록 프롬프트와 내부 지역 schema 설명을 보강했다.
모델·추론·배점·후보 내용·조건·원문 번호 선택 및 공개 HTTP 계약은 바꾸지 않았다.

동일한 서울·부산 입력의 8개 후보씩 **16개 판정이 모두 기대값과 일치**했다. API 2회는 모두 200이고,
같은 호출 결과를 production Service에 전달한 검증도 통과했다. known 판정의 실제 지역 구절 인용과
각 설명도 확인했다.

| 사례 | v1 | v2 |
|---|---|---|
| 서울 소재 / 서울 소재 또는 서울 이전 기업 | UNKNOWN — 오류 | MATCH |
| 부산 소재·이전 의사 미입력 / 위 공고 | INCOMPATIBLE — 오류 | UNKNOWN |
| 서울 소재 / 서초구 한정 | UNKNOWN | UNKNOWN |
| 본문 전국 허용 / 지역 태그만 존재 | 각각 MATCH / UNKNOWN | 유지 |

최종 16개는 v1 24개 중 서울·부산 부분집합이다. 서초구 명시 입력은 v2에서 다시 호출하지 않았다.
v1의 실패를 덮어쓰거나 24개 모두 최신 프롬프트로 재검증했다고 보고하지 않는다. 개발 중 확인한
합성 사례의 단회 재검증이며, 독립 heldout·일반 정확도·통계적 개선·실제 공고 검색의 검증이 아니다.

입력 12,080토큰, 출력 2,173토큰(합계 14,253), cached/reasoning 0. API 시간 7.875·7.219초.
8개 합성 후보의 랭킹 지연이며 20후보 실제 검색의 응답시간으로 일반화하지 않는다.
v1/v2의 동일 case `modelInputSha256`은 같고 prompt/schema 해시만 달라졌음을 기록에서 대조할 수 있다.

`fixture.json`, `capture.json`, `report.json`, `api-usage.json`, `execution-manifest.json`은 실제
실행 기록이다. `changed-sources-at-execution.json`에는 이번 prompt/agent 원문을 보존했다.
